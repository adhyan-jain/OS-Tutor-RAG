# OS-Tutor-RAG

A RAG (Retrieval-Augmented Generation) pipeline for OS course tutoring, built as a retrieval technique comparison study.

## Setup

_TODO: fill in setup instructions (environment, dependencies, data preparation, running the pipeline)._
