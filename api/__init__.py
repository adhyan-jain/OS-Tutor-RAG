"""FastAPI backend wrapping the existing RAG pipeline (src/) with a
streaming chat endpoint, model listing, and in-memory session history.

Nothing under src/ is modified by this package; api/ is purely additive.
"""
